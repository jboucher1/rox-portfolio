import { RoxScrollDirective } from './rox-scroll.directive';

describe('RoxScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RoxScrollDirective();
    expect(directive).toBeTruthy();
  });
});
